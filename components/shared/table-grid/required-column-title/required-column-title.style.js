import { defaultColors } from '../../../../style/const/style-const';

const useStyles = () => ({
  root: {
    display: 'inline-flex'
  },
  requiredSpan: {
    color: defaultColors.error,
    paddingLeft: '2px'
  }
});

export default useStyles;
